[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "pip install git+https://github.com/Dipraise1/-Engram-.git"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
