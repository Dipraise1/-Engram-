[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "engram-core"
version = "0.1.0"
requires-python = ">=3.10"
description = "Rust cryptographic core for Engram subnet — CID generation and storage proofs"

[tool.maturin]
features = ["pyo3/extension-module"]
